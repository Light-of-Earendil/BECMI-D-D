Original prompt: Skift fokus til en ren UX-gennemgang af platformen og gå direkte efter ufaerdige knapper, ufaerdige menuer, manglende visuel feedback, grimme eller uklare states og de mest synlige flowbrud foerst.

- 2026-03-06: Startet UX-spor. Plan er at inspicere den levende UI med browserautomation, identificere de mest synlige flowbrud og derefter rette de hoejeste bruger-impact problemer i smaae iterationer.
- 2026-03-06: Desktop-UX polish lagt ind. Dashboardet har nu sammenhaengende wood/parchment komposition, tomme states er gjort bevidste i stedet for ufærdige, globale knapvarianter/design tokens er udfyldt, auth-former har bedre feedback/autocomplete, og dashboardets View Calendar-knap er koblet til rigtig navigation og verificeret i browseren.

- 2026-03-08: Character creation equipment shop repaired in code. Fixed missing item category contract for Step 4 filters, added starter-friendly search/filter/card layout, widened the modal, and added a verified fallback starter catalog so equipment purchase still works when the item API fails.

- 2026-03-08: Verified character creation equipment uses central item endpoints but not the equipment-page renderer. Fixed missing modal scroll on #character-creation-content in Stylus, compiled CSS, and verified in browser that the modal content now gets overflow-y:auto and scrolls.

- 2026-03-08: Added a hard contrast guardrail to AGENTS.md to forbid light text on light parchment surfaces and dark text on dark wood surfaces. Styled character-creation HP and starting-gold result cards explicitly with dark text on light backgrounds, compiled CSS, and verified in browser that the key HP headings now render as dark wood colors instead of near-white.

- 2026-03-08: Corrected BECMI armor-class representation in UI and data defaults. Character creation, character sheet, equipment reference, and DM dashboard now show armor as fixed descending AC values and shields as negative AC adjustments; fallback/schema defaults were corrected to leather 7, chain 5, plate 3, shield -1, and sample browser checks verified the displayed labels.

- 2026-03-08: Corrected the legacy Long Sword vs Normal Sword mismatch for starter equipment. Character creation fallback now uses Rules Cyclopedia Normal Sword data, Step 4 normalizes old Long Sword rows to Normal Sword for search/rendering, and the central item endpoints now normalize legacy DB rows the same way so the central catalog can present the BECMI-correct name once the item API is healthy again.

- 2026-03-08: Collapsed bastard sword one-handed/two-handed naming into a single catalog item in character creation and item normalization. Search for `Bastard Sword`, `Bastard Sword (One-Handed)`, and `Bastard Sword (Two-Handed)` now resolves to one `Bastard Sword` entry with combined `1d6+1 / 1d8+1` damage and `1 or 2 hands` display in the verified fallback flow. Central item endpoints now also preserve legacy aliases in their normalized payloads, but live endpoint verification is still blocked by the separate `api/items/*.php` 500 error.

- 2026-03-08: Fixed portrait generation request incompatibility with Together image API. Character portrait endpoints were sending unsupported `n`, `steps`, and `512x512` parameters to `google/gemini-3-pro-image`; verified direct API response was HTTP 400 (`n is not supported`, then `steps is not supported`, then unsupported dimensions). Updated portrait endpoints to use `1024x1024` plus explicit `response_format: url`, and fixed `PortraitManager` to save under the repo's real `public/images/portraits` directory instead of a CWD-dependent `m:\\public\\images\\portraits` path. End-to-end UI verification is still pending because the local browser test context is not logged in.

- 2026-03-08: Fixed a severe character-creation UX bug where successful `Create Character` still called the discard-progress confirmation. `hideModal()` now accepts a skip-confirmation flag, and the success path calls `hideModal(true)` while cancel/discard still prompts. Verified in browser runtime with a stubbed successful create response: success path triggered `confirm` 0 times, discard path triggered it once.

- 2026-03-08: Added a shared animated dice overlay subsystem for BECMI Manager. Ported the reference wood dice art and dice/effect SFX into `public/images/dice/wood` and `public/audio/{dice,effects}`, added `public/js/core/dice-roller.js` plus Stylus source in `_dice-roller.styl`, and wired the overlay into character creation HP/gold, level-up HP, character-sheet damage rolls, and global skill/save d20 rolls. Verified runtime state in browser: overlay becomes visible mid-roll, `3d6` renders three dice, `2d4` renders two d4 dice, and chargen HP/gold plus sheet d20/damage flows return consistent results. Existing unrelated local console noise remains from Google origin config, `api/items/list.php` 500s, and missing terrain icon assets.

- 2026-03-08: Reworked the dice visuals after a concrete d20 regression report. Root issue was that the wood PNG assets are only textures, not numbered faces, so the first port hid the only visible face numbers. `public/js/core/dice-roller.js` now renders explicit die-kind and face-value overlays, and `_dice-roller.styl` now gives d6/d8/d10/d12/d20 distinct clipped silhouettes, stronger contrast, and spotlighting so the die no longer disappears into the dark shell. Browser verification confirmed the d20 skill-check path now uses `data-sides=\"20\"`, shows `kind: d20`, and all six die types render their expected side-class plus visible value in the shared overlay.

- 2026-03-08: Ported the d20 specifically closer to the Creatures & Chronicles model instead of the flat surrogate. `public/js/core/dice-roller.js` now builds a 20-face `figure.face` d20 template, and `_dice-roller.styl` now applies the reference-style `data-face` and `nth-child` transforms for the d20 geometry. Runtime verification showed the skill-check flow now produces `faceCount: 20`, `becmi-die--poly`, `shownFace` matching `data-face`, and the captured overlay screenshot `d20-overlay.png` shows a real numbered 3D d20 centered in the shell.
